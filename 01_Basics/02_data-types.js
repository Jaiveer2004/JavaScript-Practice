"use strict" // This will treat all JS code as newer version.

// alert("Hello") // We are using nodejs, not browser, so we can't use it

let name = "Jaiveer" // String
let age = 20 // Int
let isLoggedIn = true // Boolean
// a = null // Null : Standalone value : Representation of an empty value.
// b = undefined // Undefined Value

// Symbol : Symbol is used to distinct one elements from another : Unique

// Object

console.log(typeof "Jaiveer")
console.log(typeof null) // Object
console.log(typeof undefined) // Undefined

